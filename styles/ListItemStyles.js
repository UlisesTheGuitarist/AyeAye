import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  itemContainer: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 10
  },
  itemText: {
    fontSize: 18,
    flex: 1
  },
  buttonContainer: {
    flexDirection: "row",
    gap: 5
  }
});
